// Widget test placeholder — full tests to be added post-MVP.
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Placeholder test', (WidgetTester tester) async {
    // TODO: add widget tests once Firebase test harness is configured
    expect(true, isTrue);
  });
}
